import React from 'react';
import { View, Text } from 'react-native';

const OrderCanceled = ({ navigation, route }) => {
    return (
        <View style={{ justifyContent: "center", alignItems: "center" }}>
            <Text>OrderCanceled</Text>
        </View>
    );
};

export default OrderCanceled;