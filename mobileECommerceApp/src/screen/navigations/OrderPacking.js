import React from 'react';
import { View, Text } from 'react-native';

const OrderPacking = ({ navigation, route }) => {
    return (
        <View style={{ justifyContent: "center", alignItems: "center" }}>
            <Text>OrderPacking</Text>
        </View>
    );
};

export default OrderPacking;