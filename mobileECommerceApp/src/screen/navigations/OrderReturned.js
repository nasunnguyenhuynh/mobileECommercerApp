import React from 'react';
import { View, Text } from 'react-native';

const OrderReturned = ({ navigation, route }) => {
    return (
        <View style={{ justifyContent: "center", alignItems: "center" }}>
            <Text>OrderReturned</Text>
        </View>
    );
};

export default OrderReturned;